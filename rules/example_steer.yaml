# Copyright 2025-2026 .chance (dotchance)
# Licensed under the Apache License, Version 2.0. See LICENSE file.

rules:
  # Match EF-marked traffic entering eth0 and steer it toward an alternate
  # next hop. Lower priority numbers are evaluated first within steer rules.
  - name: ef-to-path-a
    priority: 10
    type: steer
    match:
      interface: eth0
      dscp: 46
      dst_ip: 10.0.0.0/8
    action:
      dst_ip: 192.168.100.1
      via: eth2
